{
  "routes": [
    {
      "method": "GET",
      "path": "/supervisor-refs",
      "handler": "supervisor-ref.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/supervisor-refs/count",
      "handler": "supervisor-ref.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/supervisor-refs/:id",
      "handler": "supervisor-ref.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/supervisor-refs",
      "handler": "supervisor-ref.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/supervisor-refs/:id",
      "handler": "supervisor-ref.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/supervisor-refs/:id",
      "handler": "supervisor-ref.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
